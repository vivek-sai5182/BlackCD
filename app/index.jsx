import { StyleSheet, Text, View } from 'react-native';
import {Color} from './constants/colors'

const index = () => {
  const ad = 1;
  return (
    

    <View>
      <Text>some showed up</Text>
    </View>
  );
};

export default index;

const styles = StyleSheet.create({});